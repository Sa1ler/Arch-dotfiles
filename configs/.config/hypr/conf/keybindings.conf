# =============================================================================
# ГОРЯЧИЕ КЛАВИШИ
# =============================================================================
# Все горячие клавиши и их привязки
# Документация: https://wiki.hypr.land/Configuring/Binds/

$mainMod = SUPER

# ----------------------------------------------------------------------------
# ЗАПУСК ПРИЛОЖЕНИЙ
# ----------------------------------------------------------------------------
bind = $mainMod, M, exec, command -v hyprshutdown >/dev/null 2>&1 && hyprshutdown || hyprctl dispatch exit
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, Q, exec, $terminal
bind = $mainMod, W, exec, $code
bind = $mainMod, X, exec, $vpn
bind = $mainMod, T, exec, $telegram
bind = $mainMod, C, killactive,
bind = $mainMod, V, togglefloating,
bind = $mainMod, A, exec, $browser
# bind = $mainMod, R, exec, rofi -show drun -show-icons -theme ~/.config/rofi/launchers/type-1/style-5.rasi
bind = $mainMod, L, exec, hyprlock
bind = $mainMod, R, exec, qs -c applauncher ipc call launcher toggle
bind = $mainMod, F, exec, ghostty -e bash -c "/home/graff/.config/fuzzy_open.sh; exec bash"
bind = $mainMod, O, exec, grim - | wl-copy
bind = $mainMod, MOUSEBTN2, togglefloating
bind = , F10, exec, qs -c screenshot ipc call screenshot toggle
bind = $mainMod, P, exec, qs -c generalmenu ipc call panel toggle
bind = $mainMod, RETURN, exec, qs -c settings ipc call settings toggle

# ----------------------------------------------------------------------------
# ЗВУК
# ----------------------------------------------------------------------------
bind = , F2, exec, ~/.config/volume-osd/scripts/volume-control.sh down
bind = , F3, exec, ~/.config/volume-osd/scripts/volume-control.sh up
bind = , XF86AudioLowerVolume, exec, ~/.config/volume-osd/scripts/volume-control.sh down
bind = , XF86AudioRaiseVolume, exec, ~/.config/volume-osd/scripts/volume-control.sh up
bind = , XF86AudioMute, exec, ~/.config/volume-osd/scripts/volume-control.sh mute
# ----------------------------------------------------------------------------
# УПРАВЛЕНИЕ ФОКУСОМ
# ----------------------------------------------------------------------------
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# ----------------------------------------------------------------------------
# ПЕРЕКЛЮЧЕНИЕ РАБОЧИХ СТОЛОВ
# ----------------------------------------------------------------------------
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# ----------------------------------------------------------------------------
# ПЕРЕМЕЩЕНИЕ ОКОН МЕЖДУ РАБОЧИМИ СТОЛАМИ
# ----------------------------------------------------------------------------
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# ----------------------------------------------------------------------------
# СПЕЦИАЛЬНЫЙ РАБОЧИЙ СТОЛ (SCRATCHPAD)
# ----------------------------------------------------------------------------
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# ----------------------------------------------------------------------------
# НАВИГАЦИЯ ПО РАБОЧИМ СТОЛАМ КОЛЁСИКОМ МЫШИ
# ----------------------------------------------------------------------------
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# ----------------------------------------------------------------------------
# ПЕРЕМЕЩЕНИЕ/ИЗМЕНЕНИЕ РАЗМЕРА ОКОН
# ----------------------------------------------------------------------------
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# ----------------------------------------------------------------------------
# МУЛЬТИМЕДИЙНЫЕ КЛАВИШИ (НОУТБУК)
# ----------------------------------------------------------------------------
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl set 10%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl set 10%-
bind = , XF86Calculator, exec, hyprshot -m output --clipboard-only

# ----------------------------------------------------------------------------
# УПРАВЛЕНИЕ ПЛЕЕРОМ (playerctl)
# ----------------------------------------------------------------------------
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

# ----------------------------------------------------------------------------
# РЕЖИМ СВОБОДНЫХ ОКОН
# ----------------------------------------------------------------------------
# Layout Edit Mode
bind = $mainMod SHIFT, SPACE, exec, ~/.config/hypr/scripts/layout-edit.sh

